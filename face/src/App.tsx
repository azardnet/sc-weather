import styled from "styled-components";

const AppWrapper = styled.section`
  padding: 40px;
`;

function App() {
  return (
    <AppWrapper>
      <div>SC-weather</div>
    </AppWrapper>
  );
}

export default App;
